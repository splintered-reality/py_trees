: ${DELIVERY_JOY_LOCATIONS:=[this, delivery, list]} # locations to deliver to when no delivery is active and the go button is pressed
: ${DELIVERY_JOY_CYCLE_DOOR:=false} # cycle doors when doing the delivery?

# Exports
export DELIVERY_JOY_LOCATIONS
export DELIVERY_JOY_CYCLE_DOOR